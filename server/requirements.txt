# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ── MongoDB async driver ───────────────────────────────────────────────────────
# 3.7.1 is required for pymongo >=4.10 compatibility (pymongo 4.17 ships with
# Python 3.14's resolver; motor 3.5.x only supports up to pymongo 4.9).
motor==3.7.1

# ── Validation & settings ─────────────────────────────────────────────────────
# Pre-release pinned: pydantic 2.14+ ships a cp314 manylinux wheel so it
# installs on Python 3.14 without needing to compile Rust from source.
--pre
pydantic>=2.14.0b1
pydantic-settings>=2.7.0

# ── Auth (JWT) ────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0

# ── Seed data generation ──────────────────────────────────────────────────────
faker==26.3.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.23.8
httpx==0.27.2
