# ── API connection ────────────────────────────────────────────────────────────
# Base URL the React app uses to reach the FastAPI backend.
# In docker-compose, this is the backend service URL as seen from the browser
# (not from inside the container network), so it uses localhost.
VITE_API_URL=http://localhost:8000
